#Our Project Folder

##Steps: 
* gem install sinatra
* npm install nodemon
* nodemon server.rb -p $PORT -o $IP  //use nodmon to start server for live preview
* create: server.rb
* write: require 'sinatra'
* write: 